# Implementation Plan: ComicInfo.xml Generation and Archive Injection\n\n## Phase 1: XML Generation Logic\n- [ ] Task: Finalize `ComicInfo.to_xml_string` and ensure it includes all necessary fields (e.g. Year).\n    - [ ] Write tests ensuring a fully populated `ComicInfo` object produces valid XML.\n    - [ ] Update `to_xml` and `from_xml` if needed to be consistent.\n- [ ] Task: Implement a utility to write the `ComicInfo.xml` content.\n    - [ ] Write tests for a helper function that saves the XML to a specific file path.\n    - [ ] Ensure UTF-8 encoding is used.\n\n## Phase 2: Archive Injection\n- [ ] Task: Implement injecting the `ComicInfo.xml` file into a `.cbz` archive.\n    - [ ] Write tests that create a mock `.cbz`, inject the XML, and verify its presence.\n    - [ ] Use `zipfile` to handle adding/updating the file in the ZIP archive.\n- [ ] Task: Verify the injected XML can be read back from the archive.\n    - [ ] Write integration tests to scan an archive, extract metadata, generate XML, inject it, and then re-read it to confirm no data loss.\n\n## Phase 3: Final Verification\n- [ ] Task: Conductor - User Manual Verification 'Phase 1 & 2 Completion' (Protocol in workflow.md)
